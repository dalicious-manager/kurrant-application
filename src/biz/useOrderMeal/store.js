import { atomWithReset } from 'jotai/utils';

export const isOrderMealAtom = atomWithReset([]);
export const todayMealAtom = atomWithReset([]);
export const isOrderMealLoadingAtom = atomWithReset(false);